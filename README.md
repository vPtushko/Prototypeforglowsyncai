
  # Prototype for GlowSync AI (Copy) (Copy)

  This is a code bundle for Prototype for GlowSync AI (Copy) (Copy). The original project is available at https://www.figma.com/design/t8l77w2dSZujn7ZjSjr9t9/Prototype-for-GlowSync-AI--Copy---Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  